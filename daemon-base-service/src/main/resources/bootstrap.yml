# Server configuration
spring:
  application:
    name: service-base
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      uri: ${cloud.configuration.uri:http://127.0.0.1:8888}
